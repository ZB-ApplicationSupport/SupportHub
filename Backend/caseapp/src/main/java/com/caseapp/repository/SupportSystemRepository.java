package com.caseapp.repository;

import com.caseapp.entity.SupportSystem;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SupportSystemRepository extends JpaRepository<SupportSystem, Long> {
}
